<Window x:Class="Media_Player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Media_Player"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <MediaElement Name="mePlayer" LoadedBehavior="Manual" MediaEnded="mePlayer_MediaEnded"/>
        <DockPanel x:Name="dPPictures">
            <ListBox x:Name="lbPreviews" DockPanel.Dock="Right" Visibility="Collapsed"/>
            <Image x:Name="imPicture"/>
        </DockPanel>
        <DockPanel x:Name="dpControlPanel" Height="60" Grid.Row="1">
            <Button x:Name="btnLast" Content="Last" DockPanel.Dock="Left" Click="btnLast_Click"/>
            <ToggleButton x:Name="btnPause" Content="Pause" DockPanel.Dock="Left" Width="35" IsEnabled="False" Checked="btnPause_Checked" Unchecked="btnPause_Unchecked"/>
            <Button x:Name="btnNext" Content="Next" DockPanel.Dock="Left" Click="btnNext_Click"/>
            <Slider x:Name="slProgress" DockPanel.Dock="Top" IsMoveToPointEnabled="True" Thumb.DragStarted="slProgress_DragStarted" 
                    Thumb.DragCompleted="slProgress_DragCompleted" ValueChanged="slProgress_ValueChanged" IsEnabled="False"/>
            <DockPanel x:Name="Controls" DockPanel.Dock="Bottom">
                <Button x:Name="btnFulscreen" Content="Fullscreen" DockPanel.Dock="Right"/>
                <Button x:Name="btnPlaylist" Content="Playlist" DockPanel.Dock="Right" Click="btnPlaylist_Click"/>
                <Button x:Name="btnOpen" Content="Open" DockPanel.Dock="Right" Click="btnOpen_Click"/>
                <RadioButton x:Name="btnVideo" Content="Video" DockPanel.Dock="Right" IsChecked="True" Click="btnVideo_Click"/>
                <RadioButton x:Name="btnAudio" Content="Audio/Picture" DockPanel.Dock="Right" Click="btnAudio_Click"/>
                <Slider x:Name="slVolume" Width="100" DockPanel.Dock="Left" Maximum="1" Value="0.7" ValueChanged="slVolume_ValueChanged"/>
                <StackPanel Orientation="Horizontal" x:Name="spSlideshow" Visibility="Collapsed">
                    <TextBlock x:Name="tbPicturesCount" Text="Počet obrázků: 0"/>
                    <TextBlock Text="Časová prodleva: "/>
                    <TextBox x:Name="tbxDelay" Text="0"/>
                    <Button x:Name="btnSlideshow" Content="Slideshow" />
                </StackPanel>
            </DockPanel>
            <TextBlock x:Name="tbProgressTime" Text="0:00:00/0:00:00" DockPanel.Dock="Right"/>
            <TextBlock x:Name="tbFilename" Text="Název souboru" HorizontalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>
